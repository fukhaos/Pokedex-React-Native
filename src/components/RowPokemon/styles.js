import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  pokemon: {
    position: 'absolute',
    width: '36%',
    height: '100%',
    right: 10,
    bottom: 20,
  },
});

export default styles;
